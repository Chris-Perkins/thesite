import * as React from "react";

const pageStyle = {
    width: "100%",
    padding: "0%",
};

export class ContactBlock extends React.Component {
    render() {
        return (
            <div style={pageStyle}>
                <p>CONTACT US</p>
            </div>
        );
    }
}