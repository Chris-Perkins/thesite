import * as React from "react";

const pageStyle = {
    margin:"0%",
    width: "100%",
    minHeight:"100%",
    padding: "0%",
    position:"absolute" as "absolute",
};

export class Home extends React.Component {
    render() {
        return (
            <div style={pageStyle}>
                <p>NOTHING!</p>
            </div>
        );
    }
}