import * as React from "react";

const pageStyle = {
    width: "100%",
    padding: "0%",
};

export class HeaderPhotoBanner extends React.Component {
    render() {
        return (
            <div style={pageStyle}>
                <p>BANNER</p>
            </div>
        );
    }
}